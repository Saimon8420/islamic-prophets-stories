import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.latifulkabir.prophetstories',
  appName: 'Prophets of Islam Stories',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
  },
};

export default config;
